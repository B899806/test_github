on: [push]
name: AzureARMSample

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
        allow-no-subscriptions: true
  

    - run: |
       az login --service-principal -u "azure-cli-2022-04-21-16-16-58" -p "fd7dbb52-0364-403a-b4fe-eca1ac9bf02d" --tenant "ab8614a3-38fa-4410-81cc-2cff09afdee1"     
       az webapp list --query "[?state=='Running']"
