on: [push]
name: AzureARMSample

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
        allow-no-subscriptions: true
  

    - run: |
       az login --service-principal -u "azuregitaction" -p "nF48Q~QulwJ4f3UsN4KCFn614SIZMrEX8bLXGcx9" --tenant "ab8614a3-38fa-4410-81cc-2cff09afdee1"     
       az webapp list --query "[?state=='Running']"
