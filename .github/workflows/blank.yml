on: [push]
name: AzureARMSample

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
        allow-no-subscriptions: true
        enable-AzPSSession: true

    - run: |
        az login --service-principal -u "9f67f27f-9c96-4f71-a384-fa6acd6f0648" -p "nF48Q~QulwJ4f3UsN4KCFn614SIZMrEX8bLXGcx9" --tenant "ab8614a3-38fa-4410-81cc-2cff09afdee1"
        az webapp list --query "[?state=='Running']"
